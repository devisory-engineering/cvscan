package main

import (
	"context"
	"fmt"
)

func runTUI(ctx context.Context) error {
	fmt.Println("TUI mode not yet implemented. Use --engagement-id, --token, and provide a repos path.")
	return nil
}
